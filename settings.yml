ocr_helper_text:
  default: "OCR ONLY — exact transcription for Discourse (discourse-math / MathJax). No Unicode. One code block only. No explanation."
  type: string
  description: "Text shown above the composer for new posts and edits."
